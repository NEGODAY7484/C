#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float x, f;
    cin >> x;
    if (x == 0) {
        f = -x;
    } else if (x > 0 && x < 2) {
        f = pow(x, 2);
    } else {
f = 4;
    }
    cout << f << endl;
    return 0;
}

