#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double m, a, F_0, u; // входные данные: масса, угол, сила, коэффициент трения
    double g = 9.81; // ускорение свободного падения
    double Ft; // сила трения
    double F; // равнодействующая сила
    double a_t; // ускорение тела
    
    cout << "Введите массу тела (кг): ";
    cin >> m;
    cout << "Введите угол (градусы): ";
    cin >> a;
    cout << "Введите силу (Н): ";
    cin >> F_0;
    cout << "Введите коэффициент трения: ";
    cin >> u;
    
    double a_r = g * sin(a); // ускорение тела, если бы не было силы трения

    // условие для проверки, движется тело или нет
    a_t = (F_0 * cos(a) - u * m * g * sin(a)) / m;
    a_t = abs(a_t) < 1e-6 ? 0 : a_t; // проверка на близость к 0
    
    F = F_0 * cos(a) - u * m * g * sin(a);
    a_t = F / m;

    cout << "Ускорение тела: " << a_t << " м/с^2" << endl;
    
    return 0;
}
