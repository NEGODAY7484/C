#include <iostream>
using namespace std;

int main() {
    int A, B, C;
    cin >> A >> B >> C;
    if (A < B && B < C || A > B && B > C) {
        cout << "True";
    } else {
        cout << "False";
    }
    return 0;
}
