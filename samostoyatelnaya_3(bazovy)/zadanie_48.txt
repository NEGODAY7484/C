#include <iostream>
using namespace std;

int main() {
    int a[10];
    int sum = 0;
    for (int i = 0; i < 10; i++) {
        cin >> a[i];
        sum += a[i] * a[i];
    }
    cout << "Сумма квадратов чисел: " << sum << endl;
    return 0;
}
